# XRPL Configuration
XRPL_NETWORK=testnet
# XRPL_BACKEND_SEED should be a valid XRPL wallet seed for the backend
# For testnet, you can generate one at: https://xrpl.org/xrp-testnet-faucet.html
# Keep this secret and never commit to version control!
XRPL_BACKEND_SEED=sEdVxxxxxxxxxxxxxxxxxxxxxxxxxx

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=shareity_db
DB_USER=root
DB_PASSWORD=password

# Server Configuration
PORT=3000
NODE_ENV=development
